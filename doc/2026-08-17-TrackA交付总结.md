# 羽联信息模块（Track A）交付总结 — 2026-08-17

> 交接文档：本模块已按计划（docs/superpowers/plans/2026-08-14-bwf-info-module.md）全部实现并通过审查。下次开发从这里接续。

## 已完成（全部本地提交、未推送）

**App 仓库** `be8eb9d..67b1550`（10 提交）：

| 提交 | 内容 |
|---|---|
| 14a0fd9 | 内置数据资产（5 单项×50 排名 + 2026 全年 30 站赛程，2026-08-17 真站抓取） |
| d370754 | Dart 模型（手写 fromJson）+ pubspec 资产注册 |
| 8188e06 | BwfDataService 三层取数：远端 raw → 本地缓存 → 内置资产 |
| 0c4dbfa | 修复缓存投毒（解码成功才写缓存 + 缓存解码失败降级资产） |
| ee58a10 | 羽联 Tab 容器（渐变头+分段）+ 排名徽章列表 + SportPalette |
| c1e6613 | 赛程月度时间轴（级别徽章/状态推导/下一站高亮） |
| 4ac89d9 | Tab1 球馆→羽联（球馆/俱乐部/大厅代码与路由保留、无 UI 入口）+ ROADMAP |
| e18ba24 | 赛程测试改相对日期（消除 2026-09-01 起的必然失败）+ 固定升序契约 |
| 67b1550 | 调试模式绕过登录直进主页（`lib/config/dev_flags.dart` 的 `devBypassLogin`，默认 kDebugMode；release 与测试不受影响） |

**工具箱仓库** `ky/tools ff88c4b..6ed267b`（5 提交）：`tools/bwf-data/` 爬虫（排名 HTML + 赛程 JSON API）+ 自包含预览页 + `run_all.py` 一键入口 + `publish.py` 发布（push 需显式 `--push`）。

**质量关**：9 任务逐任务独立审查（spec+质量双裁决）+ 最终全分支审查 + 2 项 must-fix 修复复审，全部通过。
**验证**：`flutter test` 13/13；`flutter analyze` 0 error（75 条存量 info 均在本次未触碰文件）；工具箱 `pytest` 15/15。

## 常设约定（下次开发沿用）

- **双仓库直接在 main 上开发，全程不 push**（用户 2026-08-14 明确授权；推送由用户手动决定）
- **Claude 只改代码；`flutter run` / `flutter build` 由用户本地执行**（测试可以跑）
- 周更数据：`cd ky/tools/tools/bwf-data && python run_all.py --publish`（推送另加 `--push`；不推则 App 远端层 404 自动降级缓存/内置，功能正常但拿不到更新）

## 待人工

1. **真机检查单**（Task 9 Step 3，未做）：底部 5 Tab 布局｜单项 chip 切换 + Top3 徽章 + 涨跌｜赛程时间轴 + 下一站绿框｜断网重开仍有数据｜无入口进球馆/俱乐部/大厅
2. **首次 push**：两仓库共 15 个本地提交未推
3. `.claude/settings.json`（未跟踪）：Claude 后台会话守卫开关（`worktree.bgIsolation: "none"`），保留或删除由用户定

## fix-later 清单（最终审查分诊，择机处理，非阻塞）

**工具箱**：①`validate()` 只查数量不查字段名/level 枚举（保护 Dart 解析端的最后一道门）②`main()` 无 dry_run/push 门测试③重复发布无变更时 `git commit` 裸 traceback④"-"→0 归一与 super500/300 映射无 fixture 覆盖
**App**：⑤`rank` 为 null 时 `as num` 抛异常（其余字段都有空默认；真实数据恒有 rank）⑥空单项时排名页 `keys.first` 崩溃（需自发布 JSON 穿透校验才触发，建议加 3 行空态守卫）⑦错误态不可恢复/刷新失败隐藏已载数据（服务层修复后几乎不可达）⑧`loadSchedule()` 零测试覆盖⑨`_dioGet` 生产路径无测试⑩月份高亮只比月不比年（单年数据休眠中）⑪3 个装饰性 hex 绕过 SportPalette 集中原则⑫Tab1 导航重排已有冒烟覆盖（羽联文案），可再补 MainPage 级测试

## 下次会话入口

- 用户将出新开发需求（预计 Track B 社区方向，见 ROADMAP）→ 走 brainstorming → spec → writing-plans → subagent-driven-development 流程
- Track A 数据管道已就绪，新模块可直接复用 `BwfDataService`/`SportPalette` 模式
